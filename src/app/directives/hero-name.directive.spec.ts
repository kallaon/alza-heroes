import { HeroNameDirective } from './hero-name.directive';

describe('HeroNameDirective', () => {
  it('should create an instance', () => {
    const directive = new HeroNameDirective();
    expect(directive).toBeTruthy();
  });
});
